---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Picture from '@/components/Picture.astro';
import site from '@/lib/site';
import { Image } from 'astro:assets';
import img2e781c76bde6 from '@/assets/images/2e781c76bde6.webp';
import igImg from '@/assets/images/d85622e84ef0.webp';
import fbImg from '@/assets/images/482f7439ea47.webp';
import ttImg from '@/assets/images/de7ef3d7c470.webp';
---
<BaseLayout
  title="HydraFacial Appointment Request Received | Menon Medispa"
  description="Thank you for your HydraFacial appointment request at Menon Medispa in Millburn, NJ. We will be in touch shortly."
  noindex={true}
>
  <section class="section thanks-hero">
    <div class="container--narrow thanks-hero__inner">

      <!-- Checkmark icon -->
      <div class="thanks-hero__icon" aria-hidden="true">
        <svg width="56" height="56" viewBox="0 0 56 56" fill="none" xmlns="http://www.w3.org/2000/svg">
          <circle cx="28" cy="28" r="28" fill="var(--color-cta)" fill-opacity="0.12"/>
          <path d="M16 28L24 36L40 20" stroke="var(--color-cta)" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </div>

      <h1 class="thanks-hero__heading">We Received Your HydraFacial Appointment Request</h1>

      <p class="thanks-hero__body">We will get in touch with you soon regarding your appointment details!</p>

      <p class="thanks-hero__body">In the meantime, follow us on social media to always stay up to date on our services and specials.</p>

      <div class="thanks-hero__social" role="list">
        <a href={site.social.instagram} aria-label="Instagram" target="_blank" rel="noopener" role="listitem">
          <Image src={igImg} alt="Instagram" width={25} height={25} loading="lazy" />
        </a>
        <a href={site.social.facebook} aria-label="Facebook" target="_blank" rel="noopener" role="listitem">
          <Image src={fbImg} alt="Facebook" width={25} height={25} loading="lazy" />
        </a>
        <a href={site.social.tiktok} aria-label="TikTok" target="_blank" rel="noopener" role="listitem">
          <Image src={ttImg} alt="TikTok" width={25} height={25} loading="lazy" />
        </a>
      </div>

      <Picture
        src={img2e781c76bde6}
        alt="HydraFacial treatment"
        width={570}
        height={512}
        style="margin-top:2rem;max-width:100%;border-radius:4px;"
      />

      <a href="/" class="btn btn--primary btn--lg thanks-hero__cta">Return Home</a>

    </div>
  </section>
</BaseLayout>

<style>
  .thanks-hero {
    min-height: 60vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
  }

  .thanks-hero__inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-md);
  }

  .thanks-hero__icon {
    margin-bottom: 8px;
  }

  .thanks-hero__heading {
    font-family: var(--font-display);
    font-size: var(--text-h1);
    font-weight: var(--weight-regular);
    margin: 0;
    line-height: 1.15;
  }

  .thanks-hero__body {
    font-family: var(--font-body);
    font-size: var(--text-base);
    line-height: 1.75;
    margin: 0;
    opacity: 0.82;
  }

  .thanks-hero__social {
    display: flex;
    gap: 12px;
    align-items: center;
    justify-content: center;
  }

  .thanks-hero__cta {
    margin-top: var(--space-xs);
  }

  @media (max-width: 768px) {
    .thanks-hero__heading {
      font-size: var(--text-h2);
    }

    .thanks-hero__cta {
      width: 100%;
      text-align: center;
    }
  }

  @media (max-width: 600px) {
    .thanks-hero {
      min-height: 50vh;
    }
  }
</style>
