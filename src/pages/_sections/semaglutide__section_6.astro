---
---

<section class="section sema-s6-intro">
  <div class="container container--narrow sema-s6__inner">

    <h3 class="sema-s6__heading">Begin Your Transformation Today</h3>

    <p class="sema-s6__body">
      Don't let another day pass struggling with ineffective diets and disappointing results. The science-backed solution you've been searching for is just one appointment away. Our Semaglutide program has helped countless patients achieve what they once thought impossible.
    </p>

  </div>
</section>

<style>
  .sema-s6__inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-md);
    text-align: center;
  }

  .sema-s6__heading {
    font-family: var(--font-display);
    font-size: var(--text-h2);
    font-weight: var(--weight-regular);
    color: var(--color-text);
    margin: 0;
    line-height: 1.3;
  }

  .sema-s6__body {
    font-family: var(--font-body);
    font-size: var(--text-sm);
    font-weight: var(--weight-regular);
    color: var(--color-text);
    margin: 0;
    line-height: 1.6;
  }

  @media (max-width: 768px) {
    .sema-s6__heading {
      font-size: var(--text-h3);
    }
  }
</style>
