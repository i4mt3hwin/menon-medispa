---
{/* BREAKDANCE: hand-authored from structure.json */}
import site from '@/lib/site';

import staffImg from '@/assets/images/b1a819f35921.webp';
---

<section class="section section--dark s9-cta">
  <div class="container">
    <div class="s9-cta__layout">

      <div class="s9-cta__text">

        <h2 class="s9-cta__heading">
          Not Sure If Microneedling Is Right for You?
        </h2>

        <p class="s9-cta__body">
          Schedule a complimentary consultation with our skin experts. We'll assess your skin concerns, answer all your questions, and create a personalized treatment plan, with no obligation.
        </p>

        <div class="s9-cta__actions">
          <a
            href={site.calcomUrl}
            class="btn btn--primary btn--lg"
          >
            Schedule Free Consultation
          </a>
        </div>

      </div>

      <div class="s9-cta__img-col">
        <img
          src={staffImg.src}
          alt="Menon Medispa office staff in Millburn, NJ"
          class="s9-cta__img"
          loading="lazy"
          decoding="async"
          width="431"
          height="560"
        />
      </div>

    </div>
  </div>
</section>

<style>
  .s9-cta__layout {
    display: flex;
    align-items: center;
    gap: var(--space-lg);
  }

  .s9-cta__text {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .s9-cta__heading {
    font-family: var(--font-display);
    font-size: var(--text-h2);
    font-weight: var(--weight-regular);
    color: #fff;
    margin: 0;
    line-height: 1.3;
  }

  .s9-cta__body {
    font-family: var(--font-body);
    font-size: var(--text-sm);
    color: rgba(255, 255, 255, 0.88);
    margin: 0;
    line-height: 1.6;
  }

  .s9-cta__actions {
    margin-top: var(--space-xs);
  }

  .s9-cta__img-col {
    flex-shrink: 0;
    width: 380px;
  }

  .s9-cta__img {
    width: 100%;
    height: auto;
    display: block;
    border-radius: var(--radius-sm);
    object-fit: cover;
    box-shadow: 0 8px 32px rgb(0 0 0 / 0.4);
  }

  @media (max-width: 1024px) {
    .s9-cta__img-col {
      width: 300px;
    }
  }

  @media (max-width: 768px) {
    .s9-cta__layout {
      flex-direction: column;
    }

    .s9-cta__img-col {
      width: 100%;
      max-width: 281px;
      margin: 0 auto;
    }

    .s9-cta__actions .btn {
      width: 100%;
      text-align: center;
    }
  }
</style>
