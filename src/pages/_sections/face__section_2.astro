---
---

<section class="section section--tight section--tint face-trust">
  <div class="container">
    <ul class="face-trust__bar">
      <li class="face-trust__item">
        <span class="face-trust__icon" aria-hidden="true">&#10003;</span>
        Same-Week Appointments
      </li>
      <li class="face-trust__divider" aria-hidden="true"></li>
      <li class="face-trust__item">
        <span class="face-trust__icon" aria-hidden="true">&#10003;</span>
        Led by Dr. Aditi Menon, MD
      </li>
      <li class="face-trust__divider" aria-hidden="true"></li>
      <li class="face-trust__item">
        <span class="face-trust__icon" aria-hidden="true">&#10003;</span>
        State of the Art Equipment
      </li>
    </ul>
  </div>
</section>

<style>
  .face-trust__bar {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: var(--space-sm);
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .face-trust__item {
    display: flex;
    align-items: center;
    gap: 8px;
    font-family: var(--font-ui);
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--color-text);
    letter-spacing: 0.02em;
    white-space: nowrap;
  }

  .face-trust__icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 22px;
    height: 22px;
    border-radius: 50%;
    background: var(--color-cta);
    font-size: 13px;
    color: var(--color-text);
    flex-shrink: 0;
  }

  .face-trust__divider {
    width: 1px;
    height: 24px;
    background: rgba(0, 0, 0, 0.18);
    flex-shrink: 0;
  }

  @media (max-width: 600px) {
    .face-trust__bar {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--space-xs);
    }

    .face-trust__divider {
      display: none;
    }
  }
</style>
