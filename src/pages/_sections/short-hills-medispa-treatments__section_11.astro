---
import brotoxImg from '@/assets/images/56a3e3ac1335.jpg';
import hydrafacialImg from '@/assets/images/86072f9cbd9c.jpg';
import ivImg from '@/assets/images/dd355e57f3dd.jpg';
---

<!-- Section 11: Injected promotional lightbox, rendered as an inline promo section -->
<section class="section section--tint sh11-promos">
  <div class="container">

    <h2 class="sh11-promos__heading">June SALES</h2>

    <div class="sh11-promos__grid">

      <div class="sh11-promos__card">
        <div class="sh11-promos__card-img-wrap">
          <img
            src={brotoxImg.src}
            alt="BRO-tox Fathers Day Special"
            class="sh11-promos__card-img"
            loading="lazy"
            decoding="async"
            width="150"
            height="147"
          />
        </div>
        <div class="sh11-promos__card-body">
          <h3 class="sh11-promos__card-title">BRO-tox Fathers Day Special</h3>
          <p class="sh11-promos__card-includes">Includes:</p>
          <ul class="sh11-promos__card-list">
            <li>Botox $12 Per Unit</li>
            <li>Smooths forehead, frown lines &amp; crow's feet</li>
            <li>Personalized Treatment Plan with Dr. Menon</li>
          </ul>
        </div>
      </div>

      <div class="sh11-promos__card">
        <div class="sh11-promos__card-img-wrap">
          <img
            src={hydrafacialImg.src}
            alt="Summer Glow HydraFacial"
            class="sh11-promos__card-img"
            loading="lazy"
            decoding="async"
            width="150"
            height="147"
          />
        </div>
        <div class="sh11-promos__card-body">
          <h3 class="sh11-promos__card-title">Summer Glow HydraFacial</h3>
          <ul class="sh11-promos__card-list">
            <li>Vitamin C Booster +</li>
            <li>Discount Dermaplaning $25</li>
            <li>($285 Value for $225)</li>
          </ul>
        </div>
      </div>

      <div class="sh11-promos__card">
        <div class="sh11-promos__card-img-wrap">
          <img
            src={ivImg.src}
            alt="Custom Hydrating IV"
            class="sh11-promos__card-img"
            loading="lazy"
            decoding="async"
            width="150"
            height="147"
          />
        </div>
        <div class="sh11-promos__card-body">
          <h3 class="sh11-promos__card-title">Custom Hydrating IV</h3>
          <ul class="sh11-promos__card-list">
            <li>$99 Special Price</li>
            <li>Includes any two vitamins</li>
            <li>Hydrate, recharge, glow</li>
          </ul>
        </div>
      </div>

    </div>

    <div class="sh11-promos__cta">
      <a href="/sale" class="btn btn--primary btn--lg">
        Explore all sales
      </a>
    </div>

  </div>
</section>

<style>
  .sh11-promos__heading {
    font-family: var(--font-display);
    font-size: var(--text-h2);
    font-weight: var(--weight-regular);
    color: var(--color-text);
    margin: 0 0 var(--space-lg);
    line-height: 1.3;
    text-align: center;
  }

  .sh11-promos__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-md);
    width: 100%;
  }

  .sh11-promos__card {
    display: flex;
    flex-direction: column;
    border: 1px solid rgba(0, 0, 0, 0.08);
    border-radius: var(--radius-sm);
    overflow: hidden;
    background: var(--color-bg);
    box-shadow: 0 2px 12px rgb(0 0 0 / 0.05);
  }

  .sh11-promos__card-img-wrap {
    width: 100%;
    aspect-ratio: 4 / 3;
    overflow: hidden;
  }

  .sh11-promos__card-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.3s ease;
  }

  .sh11-promos__card:hover .sh11-promos__card-img {
    transform: scale(1.04);
  }

  .sh11-promos__card-body {
    padding: var(--space-sm);
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
    flex: 1;
  }

  .sh11-promos__card-title {
    font-family: var(--font-ui);
    font-size: var(--text-sm);
    font-weight: var(--weight-bold);
    color: var(--color-text);
    margin: 0;
    line-height: 1.3;
  }

  .sh11-promos__card-includes {
    font-family: var(--font-body);
    font-size: 13px;
    color: var(--color-text);
    margin: 0;
  }

  .sh11-promos__card-list {
    list-style: disc;
    margin: 0;
    padding: 0 0 0 1.2em;
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .sh11-promos__card-list li {
    font-family: var(--font-body);
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.5;
  }

  .sh11-promos__cta {
    display: flex;
    justify-content: center;
    margin-top: var(--space-lg);
  }

  @media (max-width: 900px) {
    .sh11-promos__grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  @media (max-width: 600px) {
    .sh11-promos__grid {
      grid-template-columns: 1fr;
      max-width: 420px;
      margin-inline: auto;
    }

    .sh11-promos__cta .btn {
      width: 100%;
      text-align: center;
    }
  }
</style>
