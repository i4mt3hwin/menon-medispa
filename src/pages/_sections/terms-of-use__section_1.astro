---
---

<section class="section section--mauve tou-hero">
  <div class="container--narrow tou-hero__inner">
    <h1 class="tou-hero__heading">Terms of Use</h1>
  </div>
</section>

<style>
  .tou-hero__inner {
    text-align: center;
    padding-block: var(--space-xl);
  }

  .tou-hero__heading {
    font-family: var(--font-display);
    font-size: var(--text-h1);
    font-weight: 400;
    color: #fff;
    margin: 0;
    line-height: 1.2;
    text-wrap: balance;
  }

  @media (max-width: 768px) {
    .tou-hero__heading {
      font-size: var(--text-h2);
    }
  }
</style>
