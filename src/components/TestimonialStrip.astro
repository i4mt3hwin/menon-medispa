---
/**
 * TestimonialStrip.astro — Generated by path-2-rebuild/02-synthesize (scaffold stub).
 * Token-driven faithful stub: correct prop shape + styling hooks wired to the
 * @theme tokens (see 03-design/design-tokens.md). The first Phase 3 subagent to
 * render this pattern completes it with VERBATIM captured content. Do NOT put
 * page-specific content here.
 */
interface Props {
  heading?: string;
  class?: string;
}
const { heading } = Astro.props;
---

<section class="testimonials">
  {heading && <h2>{heading}</h2>}
  <div class="testimonials__grid"><slot /></div>
</section>

<style>
  .testimonials { padding: var(--spacing-section) 1.5rem; max-width: var(--container-max); margin: 0 auto; }
  .testimonials__grid { display: grid; gap: var(--spacing-gap-lg); grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); }
</style>
