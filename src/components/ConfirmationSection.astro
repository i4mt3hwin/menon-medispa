---
/**
 * ConfirmationSection.astro — Generated by path-2-rebuild/02-synthesize (scaffold stub).
 * Token-driven faithful stub: correct prop shape + styling hooks wired to the
 * @theme tokens (see 03-design/design-tokens.md). The first Phase 3 subagent to
 * render this pattern completes it with VERBATIM captured content. Do NOT put
 * page-specific content here.
 */
interface Props {
  heading: string;
  body?: string[];
  class?: string;
}
const { heading, body = [] } = Astro.props;
---

<section class="confirmation">
  <h1>{heading}</h1>
  {body.map((p) => <p>{p}</p>)}
</section>

<style>
  .confirmation { max-width: var(--container-content); margin: 0 auto; padding: var(--spacing-section-lg) 1.5rem; text-align: center; font-family: var(--font-ui); }
</style>
