---
/**
 * ServicesGrid.astro — Generated by path-2-rebuild/02-synthesize (scaffold stub).
 * Token-driven faithful stub: correct prop shape + styling hooks wired to the
 * @theme tokens (see 03-design/design-tokens.md). The first Phase 3 subagent to
 * render this pattern completes it with VERBATIM captured content. Do NOT put
 * page-specific content here.
 */
interface Props {
  heading?: string;
  columns?: number;
  class?: string;
}
const { heading, columns = 3 } = Astro.props;
---

<section class="services-grid">
  {heading && <h2>{heading}</h2>}
  <div class="services-grid__grid" style={`grid-template-columns:repeat(${columns},1fr)`}>
    <slot />
  </div>
</section>

<style>
  .services-grid { padding: var(--spacing-section) 1.5rem; max-width: var(--container-max); margin: 0 auto; }
  .services-grid__grid { display: grid; gap: var(--spacing-gap-lg); }
  @media (max-width: 767px) { .services-grid__grid { grid-template-columns: 1fr !important; } }
</style>
