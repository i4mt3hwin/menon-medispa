---
/**
 * FeatureList.astro — Generated by path-2-rebuild/02-synthesize (scaffold stub).
 * Token-driven faithful stub: correct prop shape + styling hooks wired to the
 * @theme tokens (see 03-design/design-tokens.md). The first Phase 3 subagent to
 * render this pattern completes it with VERBATIM captured content. Do NOT put
 * page-specific content here.
 */
interface Props {
  heading?: string;
  items: string[];
  class?: string;
}
const { heading, items = [] } = Astro.props;
---

<div class="feature-list">
  {heading && <h3>{heading}</h3>}
  <ul>{items.map((i) => <li>{i}</li>)}</ul>
</div>

<style>
  .feature-list { font-family: var(--font-ui); }
  .feature-list ul { list-style: none; padding: 0; }
  .feature-list li { padding-left: 1.5rem; position: relative; margin-bottom: 0.5rem; }
  .feature-list li::before { content: ""; position: absolute; left: 0; top: 0.55em; width: 6px; height: 6px; border-radius: var(--radius-full); background: var(--color-primary); }
</style>
