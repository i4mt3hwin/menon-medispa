---
/**
 * CtaBanner.astro — Generated by path-2-rebuild/02-synthesize (scaffold stub).
 * Token-driven faithful stub: correct prop shape + styling hooks wired to the
 * @theme tokens (see 03-design/design-tokens.md). The first Phase 3 subagent to
 * render this pattern completes it with VERBATIM captured content. Do NOT put
 * page-specific content here.
 */
interface Props {
  heading: string;
  body?: string;
  background?: string;
  class?: string;
}
const { heading, body, background } = Astro.props;
---

<section class="cta-banner" style={`background:${background ?? "var(--color-primary)"}`}>
  <h2>{heading}</h2>
  {body && <p>{body}</p>}
  <div class="cta-banner__actions"><slot /></div>
</section>

<style>
  .cta-banner { text-align: center; padding: var(--spacing-section) 1.5rem; color: var(--color-text-on-dark); }
  .cta-banner h2 { color: var(--color-text-on-dark); }
  .cta-banner__actions { display: flex; gap: var(--spacing-gap-md); justify-content: center; margin-top: var(--spacing-gap-md); flex-wrap: wrap; }
</style>
