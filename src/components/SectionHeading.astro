---
/**
 * SectionHeading.astro — Generated by path-2-rebuild/02-synthesize (scaffold stub).
 * Token-driven faithful stub: correct prop shape + styling hooks wired to the
 * @theme tokens (see 03-design/design-tokens.md). The first Phase 3 subagent to
 * render this pattern completes it with VERBATIM captured content. Do NOT put
 * page-specific content here.
 */
interface Props {
  heading: string;
  heading_tag?: "h2" | "h3";
  subheading?: string;
  align?: "center" | "left";
  class?: string;
}
const { heading, heading_tag = "h2", subheading, align = "center" } = Astro.props;
---

<div class={`section-heading section-heading--${align}`}>
  {heading_tag === "h2" ? <h2>{heading}</h2> : <h3>{heading}</h3>}
  {subheading && <p>{subheading}</p>}
</div>

<style>
  .section-heading { padding: var(--spacing-band) 1.5rem; max-width: var(--container-content); margin: 0 auto; }
  .section-heading--center { text-align: center; }
</style>
