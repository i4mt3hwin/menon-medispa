---
/**
 * ContactBlock.astro — Generated by path-2-rebuild/02-synthesize (scaffold stub).
 * Token-driven faithful stub: correct prop shape + styling hooks wired to the
 * @theme tokens (see 03-design/design-tokens.md). The first Phase 3 subagent to
 * render this pattern completes it with VERBATIM captured content. Do NOT put
 * page-specific content here.
 */
interface Props {
  heading?: string;
  showForm?: boolean;
  class?: string;
}
const { heading, showForm = false } = Astro.props;
---

<section class="contact-block">
  {heading && <h2>{heading}</h2>}
  <slot />
</section>

<style>
  .contact-block { padding: var(--spacing-section) 1.5rem; max-width: var(--container-max); margin: 0 auto; font-family: var(--font-ui); }
</style>
