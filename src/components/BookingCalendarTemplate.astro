---
/**
 * BookingCalendarTemplate.astro — Generated by path-2-rebuild/02-synthesize (scaffold stub).
 * Token-driven faithful stub: correct prop shape + styling hooks wired to the
 * @theme tokens (see 03-design/design-tokens.md). The first Phase 3 subagent to
 * render this pattern completes it with VERBATIM captured content. Do NOT put
 * page-specific content here.
 */
interface Props {
  title: string;
  intro?: string;
  serviceSlug?: string;
  class?: string;
}
const { title, intro, serviceSlug } = Astro.props;
---

<section class="booking-calendar">
  <h1>{title}</h1>
  {intro && <p>{intro}</p>}
  <div class="booking-calendar__placeholder">Appointment request form (wired in a later phase).</div>
</section>

<style>
  .booking-calendar { max-width: var(--container-content); margin: 0 auto; padding: var(--spacing-section) 1.5rem; font-family: var(--font-ui); text-align: center; }
</style>
